apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-oidc-secret
spec:
  itemPath: {{ required "A secret-reference for the OIDC-Secret must be provided" .Values.secrets.oidcSecretRef }}
