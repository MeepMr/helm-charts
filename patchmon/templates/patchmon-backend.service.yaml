apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-backend-service
spec:
  selector:
    app: {{ .Release.Name }}-{{ .Chart.Name }}-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      name: http
  type: ClusterIP
