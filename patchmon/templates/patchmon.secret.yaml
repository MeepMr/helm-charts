apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-patchmon-secret
spec:
  itemPath: {{ required "A secret-reference for the Patchmon-Secret must be provided" .Values.secrets.patchmonSecretRef }}
