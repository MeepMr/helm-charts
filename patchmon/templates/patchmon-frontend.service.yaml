apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-frontend-service
spec:
  selector:
    app: {{ .Release.Name }}-{{ .Chart.Name }}-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      name: http
  type: ClusterIP
