apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-service
spec:
  selector:
    app: {{ .Release.Name }}-{{ .Chart.Name }}-sfs
  ports:
    - protocol: TCP
      port: 80
      targetPort: paperless-port
      name: pless-s-port
  type: ClusterIP
