apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-secret
spec:
  itemPath: {{ required "You must provide a xxxSecret Reference" .Values.secrets.xxxSecretRef }}
