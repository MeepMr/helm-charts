name: Publish Zipline Helm Chart
on:
  workflow_dispatch:
  push:
    paths:
      - satisfactory/**
jobs:
  publish-satisfactory:
    name: Publish Satisfactory Helm Chart
    uses: ./.github/workflows/publish-helm-chart.yml
    with:
      chart-name: 'satisfactory'
    secrets: inherit
#  deploy-satisfactory:
#    name: Deploy Satisfactory Helm Chart
#    needs: publish-satisfactory
#    uses: ./.github/workflows/deploy-helm-chart.yml
#    with:
#      deployment-identifier: 'satisfactory'
#      chart-version: ${{ needs.publish-satisfactory.outputs.chart-version }}
#    secrets: inherit
